# ============================================================================
# Generated by httpxgen. DO NOT EDIT.
# ============================================================================


from collections.abc import Mapping
from typing import Self
from uuid import UUID

import httpx
from pydantic import TypeAdapter

from generated.control_plane.exceptions import ApiError
from generated.control_plane.http_methods import HttpMethods
from generated.control_plane.models import (
    BrowserCapacityExhaustedError,
    BrowserNotFoundError,
    BrowserResponse,
    BrowserUnavailableError,
    CreateLeaseRequest,
    HTTPValidationError,
    LeaseNotFoundError,
    LeaseResponse,
)
from generated.control_plane.serialization import serialize_path


class GeneratedControlPlaneClient:
    def __init__(
        self,
        client: httpx.AsyncClient,
        base_url: str,
        *,
        headers: Mapping[str, str] | None = None,
        credentials: Mapping[str, str | tuple[str, str]] | None = None,
        timeout: float = 30.0,
    ) -> None:
        self._client = client
        self._base_url = base_url.rstrip("/")
        self._headers = dict(headers) if headers else {}
        self._credentials = dict(credentials) if credentials else {}
        self._timeout = timeout

    async def aclose(self) -> None:
        await self._client.aclose()

    async def __aenter__(self) -> Self:
        return self

    async def __aexit__(self, *exc_info: object) -> None:
        await self.aclose()

    async def list_browsers(
        self,
        *,
        timeout: float | None = None,
    ) -> list[BrowserResponse]:
        path = "/api/v1/browsers"

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.GET,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 200:
            return TypeAdapter(list[BrowserResponse]).validate_python(response.json())

        raise ApiError(response.status_code, response.text, response=response)

    async def create_browser(
        self,
        *,
        timeout: float | None = None,
    ) -> BrowserResponse:
        path = "/api/v1/browsers"

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.POST,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 201:
            return BrowserResponse.model_validate(response.json())
        if response.status_code == 409:
            parsed_body = BrowserCapacityExhaustedError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def get_browser(
        self,
        browser_id: UUID,
        *,
        timeout: float | None = None,
    ) -> BrowserResponse:
        path = "/api/v1/browsers/{browser_id}"
        path = path.replace("{browser_id}", serialize_path("browser_id", browser_id))

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.GET,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 200:
            return BrowserResponse.model_validate(response.json())
        if response.status_code == 404:
            parsed_body = BrowserNotFoundError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 422:
            parsed_body = HTTPValidationError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def destroy_browser(
        self,
        browser_id: UUID,
        *,
        timeout: float | None = None,
    ) -> None:
        path = "/api/v1/browsers/{browser_id}"
        path = path.replace("{browser_id}", serialize_path("browser_id", browser_id))

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.DELETE,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 204:
            return None
        if response.status_code == 404:
            parsed_body = BrowserNotFoundError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 422:
            parsed_body = HTTPValidationError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def reset_browser(
        self,
        browser_id: UUID,
        *,
        timeout: float | None = None,
    ) -> BrowserResponse:
        path = "/api/v1/browsers/{browser_id}/reset"
        path = path.replace("{browser_id}", serialize_path("browser_id", browser_id))

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.POST,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 200:
            return BrowserResponse.model_validate(response.json())
        if response.status_code == 404:
            parsed_body = BrowserNotFoundError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 422:
            parsed_body = HTTPValidationError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def health(
        self,
        *,
        timeout: float | None = None,
    ) -> dict[str, str]:
        path = "/api/v1/health"

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.GET,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 200:
            return TypeAdapter(dict[str, str]).validate_python(response.json())

        raise ApiError(response.status_code, response.text, response=response)

    async def create_lease(
        self,
        body: CreateLeaseRequest,
        *,
        timeout: float | None = None,
    ) -> LeaseResponse:
        path = "/api/v1/leases"

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        json_body = TypeAdapter(CreateLeaseRequest).dump_python(
            body, mode="json", by_alias=True, exclude_none=True
        )

        response = await self._client.request(
            method=HttpMethods.POST,
            url=f"{self._base_url}{path}",
            headers=headers,
            json=json_body,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 201:
            return LeaseResponse.model_validate(response.json())
        if response.status_code == 404:
            parsed_body = BrowserNotFoundError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 409:
            parsed_body = BrowserUnavailableError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 422:
            parsed_body = HTTPValidationError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def get_lease(
        self,
        lease_id: UUID,
        *,
        timeout: float | None = None,
    ) -> LeaseResponse:
        path = "/api/v1/leases/{lease_id}"
        path = path.replace("{lease_id}", serialize_path("lease_id", lease_id))

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.GET,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 200:
            return LeaseResponse.model_validate(response.json())
        if response.status_code == 404:
            parsed_body = LeaseNotFoundError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 422:
            parsed_body = HTTPValidationError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def release_lease(
        self,
        lease_id: UUID,
        *,
        timeout: float | None = None,
    ) -> None:
        path = "/api/v1/leases/{lease_id}"
        path = path.replace("{lease_id}", serialize_path("lease_id", lease_id))

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.DELETE,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 204:
            return None
        if response.status_code == 404:
            parsed_body = LeaseNotFoundError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)
        if response.status_code == 422:
            parsed_body = HTTPValidationError.model_validate(response.json())
            raise ApiError(response.status_code, response.text, parsed_body, response)

        raise ApiError(response.status_code, response.text, response=response)

    async def readiness(
        self,
        *,
        timeout: float | None = None,
    ) -> dict[str, str]:
        path = "/api/v1/readiness"

        headers = dict(self._headers)
        headers.setdefault("Accept", "application/json")

        response = await self._client.request(
            method=HttpMethods.GET,
            url=f"{self._base_url}{path}",
            headers=headers,
            timeout=self._timeout if timeout is None else timeout,
        )

        if response.status_code == 200:
            return TypeAdapter(dict[str, str]).validate_python(response.json())

        raise ApiError(response.status_code, response.text, response=response)
